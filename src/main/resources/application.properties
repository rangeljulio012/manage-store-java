# Server
server.port=8080

spring.config.import=optional:file:.env.properties[.properties]

# Database

spring.datasource.url=jdbc:mysql://root:swMwqDXMLflXeflMvufyhrjXbnTbnQlT@metro.proxy.rlwy.net:53602/railway?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Security (configuraci�n b�sica)
spring.security.user.name=admin
spring.security.user.password=${ADMIN_PASSWORD}
jwt.secret=${JWT_SECRET}
jwt.expiration.access=${JWT_ACCESS_EXPIRATION}
jwt.expiration.refresh=${JWT_REFRESH_EXPIRATION}
